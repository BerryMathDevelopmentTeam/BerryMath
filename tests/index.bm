import "sys" as sys;// 导入系统库

let n = sys.type.number(sys.io.input("N: ", sp = ""));// 接受输入, 并将其转为数字

if (n == 0) {
    sys.io.print("n == 0");
} elif (n == 1) {
    sys.io.print("n == 1");
} else {
    sys.io.print("another");
}